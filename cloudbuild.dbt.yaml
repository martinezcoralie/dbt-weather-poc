steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-f', 'cloud/run/jobs/dbt/Dockerfile',
      '-t', 'europe-west1-docker.pkg.dev/dbt-weather-poc/weather/dbt:latest',
      '.'
    ]
images:
  - 'europe-west1-docker.pkg.dev/dbt-weather-poc/weather/dbt:latest'
