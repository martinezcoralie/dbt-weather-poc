steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-f', 'cloud/run/jobs/ingest/Dockerfile',
      '-t', 'europe-west1-docker.pkg.dev/dbt-weather-poc/weather/ingest:latest',
      '.'
    ]
images:
  - 'europe-west1-docker.pkg.dev/dbt-weather-poc/weather/ingest:latest'
